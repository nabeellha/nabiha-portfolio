"use client";
import { useState } from "react";
export function AdminLoginForm(){const[message,setMessage]=useState<string>();const[busy,setBusy]=useState(false);async function submit(form:FormData){setBusy(true);setMessage();const response=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:form.get("email"),password:form.get("password"),remember:form.get("remember")==="on"})});setBusy(false);setMessage(response.ok?"Signed in successfully.":response.status===429?"Too many attempts. Please try again later.":"Invalid email or password.")}const input="focus-ring rounded-xl border border-white/10 bg-slate-950/50 px-4 py-3";return <form action={submit} className="mt-6 grid gap-4"><label className="grid gap-2 text-sm">Email<input name="email" type="email" className={input} autoComplete="email" required/></label><label className="grid gap-2 text-sm">Password<input name="password" type="password" className={input} autoComplete="current-password" required/></label><label className="flex items-center gap-2 text-sm text-slate-400"><input name="remember" type="checkbox"/>Remember me</label><button disabled={busy} className="focus-ring rounded-full bg-violet px-5 py-3 font-semibold text-white disabled:opacity-60">{busy?"Signing in…":"Sign in"}</button>{message&&<p role="status" className="text-sm text-cyan">{message}</p>}</form>}
